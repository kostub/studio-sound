{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main Studio Sound App window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
